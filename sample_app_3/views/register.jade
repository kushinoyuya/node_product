extends layout
div
p 新規登録フォーム（ユーザーアカウント登録画面）


form(action="/registers", method="post") 新規登録
    div
        label(for="") ユーザー名
        br
        input(type="text", name="username")

    div
        label(for="") メールアドレス
        br
        input(type="email", name="email")

    div
        label(for="") パスワード
        br
        input(type="password", name="password")

    div
        input(type="submit", value="登録")
